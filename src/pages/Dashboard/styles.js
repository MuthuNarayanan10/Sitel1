import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles({
    filterButton: {
        marginTop: "10px",
        marginBottom: "10px",
    },
    paper: {
        height: 300,
        width: 250,
        border: "1px solid",
        borderRadius: "5px",
    },
    infoContainer: {
        marginTop: "20px",
        flexGrow: 1,
        marginBottom: "10px",
    },
});